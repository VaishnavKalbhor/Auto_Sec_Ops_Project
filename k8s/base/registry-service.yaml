apiVersion: v1
kind: Service
metadata:
  name: vehicle-registry-service
  namespace: autosecureops
  labels:
    app: vehicle-registry-service
spec:
  selector:
    app: vehicle-registry-service
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP
